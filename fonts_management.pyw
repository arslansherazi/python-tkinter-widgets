from tkinter import *

win=Tk()
win.title("Tic Toc Toe Game")
win.geometry("500x500")

Label(text="Press To start the Game",bg="Green",fg="White",font=('Arial Black',50,'italic')).pack()

# font('font name',size,'style') it is used to manage the font

win.mainloop()     
