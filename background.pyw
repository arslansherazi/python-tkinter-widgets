from tkinter import *

win = Tk()

# this line changes the background of Window (we change the color name as well as color code in #xxxxxx form)
win.configure(background="khaki")
win.title("Tic Toc Toe Game")
win.geometry("500x500")
win.resizable(0, 0)

win.mainloop()
